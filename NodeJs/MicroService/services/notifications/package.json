{
  "name": "notifications-service",
  "version": "1.0.0",
  "description": "Notifications management — clustered across CPU cores, worker threads for heavy tasks",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    "dotenv": "^16.3.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
